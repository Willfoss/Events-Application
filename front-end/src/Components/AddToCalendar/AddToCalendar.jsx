import React from "react";
import "./addToCalendar.css";

export default function AddToCalendar(props) {
  const { event } = props;

  return <button className="add-to-calendar">Add to Calendar</button>;
}
